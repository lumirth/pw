// Cppcheck 2.21 treats unsigned short as signed int after promotion even when
// the custom platform gives both 16 bits. CH38 promotes it to unsigned int.
// The first expression adds a page to an EEPROM address. The battle subtraction
// is unsigned and its subtrahend is capped at the current watt balance.
// See docs/build.md.
integerOverflow:src/startup/h8_resetprg.c:302
integerOverflowCond:src/application/pw_battle.c:443
